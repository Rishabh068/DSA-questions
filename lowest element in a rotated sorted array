
int binarysearch(int arr[],int n){
   int start=0,end=n-1;
    while(start<end){
      int mid=(start+end)/2;
           next=mid+1%n;
       prev=(mid+n-1)%n;
      if(arr[mid]<a[prev]&&a[mid]<a[next])
         return mid;

      else if(a[mid]<a[start])
      end=mid-1;
       else
       start=mid+1;
    }
   return -1;
  }
